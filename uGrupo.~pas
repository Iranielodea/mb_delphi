unit uGrupo;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, uBase, DB, DBCtrls, Mask, ComCtrls, Grids, DBGrids, ExtCtrls,
  StdCtrls, Buttons;

type
  TfGrupo = class(TfBase)
    procedure FormActivate(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  fGrupo: TfGrupo;

implementation

uses uDM;

{$R *.dfm}

procedure TfGrupo.FormActivate(Sender: TObject);
begin
   dm.Tamanho_Form(fGrupo);
  inherited;
end;

end.
